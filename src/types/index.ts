export interface FeaturedDish {
  name: string;
  image: string;
  description: string;
  price: string;
}

export interface Review {
  name: string;
  image: string;
  rating: number;
  text: string;
}